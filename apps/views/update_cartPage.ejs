<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Cập nhật thông tin đơn hàng</title>
    <link rel="stylesheet" type="text/css" media="screen" href="/static/css/cart_manager.css" />
</head>
<body>
    <h2>Cập nhật trạng thái đơn hàng</h2>
    <div id="cap_nhat_don_hang">
        <%for(var i = data.length-1; i >= 0; i--){%>

        <div class="product-image-wrapper" id="<%= data[i].ma_don_hang%>">
        <h4>Mã đơn hàng: <%= data[i].ma_don_hang%></h4>
        <table>
            <tr>
                <th>Người nhận</th>
                <th>Người gửi</th>
                <th>Địa chỉ</th>
                <th>Số điện thoại</th>
                <th>Ngày mua</th>
            </tr>
            <tr>
                <td><%= data[i].nguoi_nhan%></td>
                <td><%= data[i].nguoi_gui%></td>
                <td><%= data[i].dia_chi%></td>
                <td><%= data[i].so_dien_thoai%></td>
                <td><%= data[i].ngay_mua%></td>
            </tr>
            </table>

            <div style="margin:20px" id="cap_nhat">
            <p>Trạng thái:</p>
            <div>
                <form>
                <%if (data[i].trang_thai == 0){%>
                    <input name="gender" value="XNDG" type="radio" style="margin: 10px;" checked/> Xác nhận đóng gói
                    <input name="gender" value="LHTK" type="radio" style="margin: 10px;"/> Lấy hàng từ kho
                    <input name="gender" value="BDGH" type="radio" style="margin: 10px;"/> Bắt đầu giao hàng
                    <input name="gender" value="HTGH" type="radio" style="margin: 10px;"/> Hoàn tất giao hàng
                <%}%>
                <%if (data[i].trang_thai == 1){%>
                    <input name="gender" value="XNDG" type="radio" style="margin: 10px;"/> Xác nhận đóng gói
                    <input name="gender" value="LHTK" type="radio" style="margin: 10px;" checked/> Lấy hàng từ kho
                    <input name="gender" value="BDGH" type="radio" style="margin: 10px;"/> Bắt đầu giao hàng
                    <input name="gender" value="HTGH" type="radio" style="margin: 10px;"/> Hoàn tất giao hàng
                <%}%>
                <%if (data[i].trang_thai == 2){%>
                    <input name="gender" value="XNDG" type="radio" style="margin: 10px;"/> Xác nhận đóng gói
                    <input name="gender" value="LHTK" type="radio" style="margin: 10px;"/> Lấy hàng từ kho
                    <input name="gender" value="BDGH" type="radio" style="margin: 10px;" checked/> Bắt đầu giao hàng
                    <input name="gender" value="HTGH" type="radio" style="margin: 10px;"/> Hoàn tất giao hàng
                <%}%>
                <%if (data[i].trang_thai == 3){%>
                    <input name="gender" value="XNDG" type="radio" style="margin: 10px;"/> Xác nhận đóng gói
                    <input name="gender" value="LHTK" type="radio" style="margin: 10px;"/> Lấy hàng từ kho
                    <input name="gender" value="BDGH" type="radio" style="margin: 10px;"/> Bắt đầu giao hàng
                    <input name="gender" value="HTGH" type="radio" style="margin: 10px;" checked/> Hoàn tất giao hàng
                <%}%>
                </form>
            </div>
            <button class="btn btn-success" id="<%= data[i].ma_don_hang%>" style="margin-left: 20px; margin: 10px;">Cập nhật</button>
        </div>
        </div>
    <%}%>
    </div>

    <script src="../static/js/staff_cart.js"></script>
</body>
</html>